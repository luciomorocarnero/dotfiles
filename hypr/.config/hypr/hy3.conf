# 1. Cargar el plugin
exec-once = hyprpm reload -n

$font = "CaskaydiaCove Nerd Font"

# 2. Definir el layout
general {
    layout = hy3
}

plugin {
  hy3 {
    # policy controlling what happens when a node is removed from a group,
    # leaving only a group
    # 0 = remove the nested group
    # 1 = keep the nested group
    # 2 = keep the nested group only if its parent is a tab group
    node_collapse_policy = 2 # default: 2

    # offset from group split direction when only one window is in a group
    group_inset = 0 # default: 10

    # if a tab group will automatically be created for the first window spawned in a workspace
    tab_first_window = false

    # tab group settings
    tabs {
      # height of the tab bar
      height = 22 # default: 22

      # padding between the tab bar and its focused node
      padding = 6 # default: 6

      # the tab bar should animate in/out from the top instead of below the window
      from_top = false # default: false

      # radius of tab bar corners
      radius = 4 # default: 6

      # tab bar border width
      border_width = 2 # default: 2

      # render the window title on the bar
      render_text = true # default: true

      # center the window title
      text_center = true # default: true

      # font to render the window title with
      text_font = $font semibold# default: Sans

      # height of the window title
      text_height = 8 # default: 8

      # left padding of the window title
      text_padding = 3 # default: 3

      # ACTIVE (tab activo)
      col.active = rgba(ffffffff)              
      col.active.border = rgba(ffffffff)      
      col.active.text = rgba(000000ff)       

      # ACTIVE EN MONITOR NO ENFOCADO
      col.active_alt_monitor = rgba(aaaaaa40)  
      col.active_alt_monitor.border = rgba(ccccccaa)
      col.active_alt_monitor.text = rgba(ffffffff)

      # FOCUSED (dentro de contenedor no enfocado)
      col.focused = rgba(ffffff20)            
      col.focused.border = rgba(ffffff80)
      col.focused.text = rgba(ffffffff)

      # INACTIVE
      col.inactive = rgba(000000ff)          
      col.inactive.border = rgba(ffffff40)  
      col.inactive.text = rgba(ffffffff)   

      # URGENT (notificaciones / ventanas importantes)
      col.urgent = rgba(ff444440)         
      col.urgent.border = rgba(ff4444ff)
      col.urgent.text = rgba(ffffffff)

      # LOCKED
      col.locked = rgba(ffff0040)        
      col.locked.border = rgba(ffff00ff)
      col.locked.text = rgba(000000ff)

      # if tab backgrounds should be blurred
      # Blur is only visible when the above colors are not opaque.
      blur = true # default: true

      # opacity multiplier for tabs
      # Applies to blur as well as the given colors.
      #opacity = <float> # default: 1.0
    }

    # autotiling settings
    autotile {
      # enable autotile
      enable = true # default: false

      # make autotile-created groups ephemeral
      ephemeral_groups = true # default: true

      # if a window would be squished smaller than this width, a vertical split will be created
      # -1 = never automatically split vertically
      # 0 = always automatically split vertically
      # <number> = pixel width to split at
      trigger_width = 0 # default: 0

      # if a window would be squished smaller than this height, a horizontal split will be created
      # -1 = never automatically split horizontally
      # 0 = always automatically split horizontally
      # <number> = pixel height to split at
      trigger_height = 0 # default: 0

      # a space or comma separated list of workspace ids where autotile should be enabled
      # it's possible to create an exception rule by prefixing the definition with "not:"
      # workspaces = 1,2 # autotiling will only be enabled on workspaces 1 and 2
      # workspaces = not:1,2 # autotiling will be enabled on all workspaces except 1 and 2
      workspaces = all # default: all
    }
  }
}
$mainMod = SUPER

# Move
bind = $mainMod, H, hy3:movefocus, l, stay_out_of_tabs
bind = $mainMod, J, hy3:movefocus, d, stay_out_of_tabs
bind = $mainMod, K, hy3:movefocus, u, stay_out_of_tabs
bind = $mainMod, L, hy3:movefocus, r, stay_out_of_tabs

bind = $mainMod, left,  hy3:movefocus, l
bind = $mainMod, down,  hy3:movefocus, d
bind = $mainMod, up,    hy3:movefocus, u
bind = $mainMod, right, hy3:movefocus, r

# Windows
bind = $mainMod SHIFT, H, hy3:movewindow, l
bind = $mainMod SHIFT, J, hy3:movewindow, d
bind = $mainMod SHIFT, K, hy3:movewindow, u
bind = $mainMod SHIFT, L, hy3:movewindow, r

bind = $mainMod SHIFT, left,  hy3:movewindow, l
bind = $mainMod SHIFT, down,  hy3:movewindow, d
bind = $mainMod SHIFT, up,    hy3:movewindow, u
bind = $mainMod SHIFT, right, hy3:movewindow, r

# Tabs

bind = $mainMod, T, hy3:changegroup, toggletab

bind = $mainMod, X, hy3:locktab, lock
bind = $mainMod SHIFT, X, hy3:locktab, unlock

# bind = $mainMod, TAB, hy3:focustab, r, wrap
# bind = $mainMod SHIFT, TAB, hy3:focustab, l, wrap
